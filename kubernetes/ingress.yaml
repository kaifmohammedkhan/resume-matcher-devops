apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: resume-matcher-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: resume.devops.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: resume-matcher-service
                port:
                  number: 80
