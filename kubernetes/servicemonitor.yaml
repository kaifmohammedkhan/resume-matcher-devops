apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: resume-matcher-monitor
spec:
  selector:
    matchLabels:
      app: resume-matcher
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
