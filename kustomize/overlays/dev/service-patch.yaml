apiVersion: v1
kind: Service
metadata:
  name: resume-matcher-service
spec:
  selector:
    app: resume-matcher-ghcr
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
    - name: metrics
      port: 51113
      targetPort: 51113